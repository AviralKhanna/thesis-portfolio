{
  "name": "investment-intelligence-system",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A local-first personal investment knowledge and company evaluation workspace.",
  "scripts": {
    "thesis": "node scripts/thesis.mjs",
    "portfolio:batch": "node scripts/portfolio-batch.mjs",
    "dev": "node backend/server.mjs",
    "start": "node backend/server.mjs",
    "test": "node --test",
    "check": "node --check backend/server.mjs && node --check scripts/thesis.mjs && node --check scripts/portfolio-batch.mjs && node --check frontend/src/app.js && node --check src/data/seed.js && node --check src/lib/engine.js && node --check src/lib/evidence.js && node --check src/lib/memory.js && node --check src/lib/portfolio-batch.js && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
